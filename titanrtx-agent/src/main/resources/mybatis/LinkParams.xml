<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunji.titanrtx.agent.mapper.LinkParamsMapper">

    <select id="selectParamsByLinkId" resultType="java.lang.String">
        SELECT lp.param FROM link_params lp
        WHERE
            lp.linkId = #{linkId}
        LIMIT 5000
    </select>

    <select id="selectParamRandom" resultType="string">
        SELECT lp.param FROM link_params lp where lp.linkId = #{linkId} and lp.deleted = 0 ORDER BY RAND() LIMIT 1
    </select>


    <select id="selectParamsBatch" resultType="java.lang.String" parameterType="list">
        SELECT lp.param FROM link_params lp where lp.id in
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item,jdbcType=NUMERIC}
        </foreach>
    </select>

    <select id="selectParamsByRange" resultType="java.lang.String">
        SELECT lp.param FROM link_params lp
        WHERE
            lp.linkId = #{linkId}
         AND
            lp.orders between #{start} and #{end}
    </select>


</mapper>

