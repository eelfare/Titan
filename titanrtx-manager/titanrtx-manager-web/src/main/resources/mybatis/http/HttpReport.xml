<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunji.titanrtx.manager.dao.mapper.http.HttpReportMapper">


    <insert id="insert">
        insert into http_report (sceneId,snap)
        values (#{sceneId},#{snap})
        <selectKey resultType="Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <delete id="deleteById">
        delete from http_report where id = #{id}
    </delete>

    <select id="selectBySceneId" resultType="com.yunji.titanrtx.manager.dao.entity.http.HttpReportEntity">
        select * from http_report r where r.sceneId = #{sceneId} order by r.updateTime desc
    </select>

    <select id="findById" resultType="com.yunji.titanrtx.manager.dao.entity.http.HttpReportEntity">
        select * from http_report where id = #{id}
    </select>

    <select id="selectAll" resultType="com.yunji.titanrtx.manager.dao.entity.http.HttpReportEntity">
        select * from http_report r order by r.updateTime desc limit 1000
    </select>


    <select id="count" resultType="integer">
        SELECT count(id) FROM http_report
    </select>


    <select id="selectByIds" resultType="com.yunji.titanrtx.manager.dao.entity.http.HttpReportEntity">
        SELECT * FROM http_report r where r.id in (
        <foreach collection="list" item="id" separator=",">
            #{id}
        </foreach>
        )
        order by r.createTime
    </select>

</mapper>

