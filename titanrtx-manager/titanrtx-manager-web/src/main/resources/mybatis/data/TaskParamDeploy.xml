<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunji.titanrtx.manager.dao.mapper.data.TaskParamDeployMapper">

    <delete id="deleteById">
        delete from task_param_deploy where id = #{id}
    </delete>

    <delete id="deleteAllByTaskId">
        delete from task_param_deploy where taskId = #{taskId}
    </delete>

    <insert id="insert">
        insert into task_param_deploy (taskId,name,extra)
        values (#{taskId},#{name}, #{extra})
        <selectKey resultType="Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <select id="findById" resultType="com.yunji.titanrtx.manager.dao.entity.data.TaskParamDeployEntity">
        SELECT * FROM task_param_deploy tpd where tpd.id = #{id}
    </select>

    <select id="selectByTaskId" resultType="com.yunji.titanrtx.manager.dao.entity.data.TaskParamDeployEntity">
        SELECT * FROM task_param_deploy tpd where tpd.taskId = #{taskId} order by tpd.createTime asc
    </select>


    <update id="update">
        update task_param_deploy tpd set tpd.name =#{name},tpd.extra =#{extra},tpd.updateTime = now() where tpd.id = #{id}
    </update>
</mapper>

